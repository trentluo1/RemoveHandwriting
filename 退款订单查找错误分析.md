# Stripe Webhook 退款订单查找错误分析

## 问题描述

在处理 `charge.refunded` webhook 事件时，系统无法找到对应的订单：

```
Charge refunded: {
  chargeId: 'ch_3SLdWIHEruuV5CvF1Ekj8bPC',
  amount: 2780,
  refundId: undefined,
  paymentIntent: 'pi_3SLdWIHEruuV5CvF1vs1b8xU'
}
No order found for charge refund: ch_3SLdWIHEruuV5CvF1Ekj8bPC
```

## 订单信息（从 Stripe 页面获取）

- **Customer ID**: `cus_TIDyHolVhQ1501`
- **Customer Email**: `kontakt@schauertegmbh.de`
- **Subscription ID**: `sub_1SLdWKHEruuV5CvF70FmZNrc`
- **Price ID**: `price_1SHKFFHEruuV5CvFCetrtCPO`
- **订单类型**: 首次订阅 (Subscription creation)
- **金额**: $27.80 (2780 分)
- **创建时间**: 2025-10-24 05:26 (UTC+8)
- **退款时间**: 2026-01-08 07:37 (UTC+8)

## 根本原因分析

### 1. 订单创建时的存储逻辑

查看 `removehandwriting-web/src/app/api/stripe/webhook/route.ts` 中的订单创建代码：

**首次订阅订单**（`checkout.session.completed` 事件，第552行）：
```typescript
await pool.query(
  `INSERT INTO "Order" (..., "stripeSessionId", ...)
   VALUES (..., $4, ...)`,
  [..., session.id, ...]  // ✅ 存储的是 checkout session ID (cs_xxx)
)
```

**续订订单**（`invoice.payment_succeeded`，billing_reason='subscription_cycle'，第944行）：
```typescript
await pool.query(
  `INSERT INTO "Order" (..., "stripeSessionId", ...)
   VALUES (..., $4, ...)`,
  [..., invoice.id, ...]  // ✅ 存储的是 invoice ID (in_xxx)
)
```

**升级订单**（`invoice.payment_succeeded`，billing_reason='subscription_update'，第1031行）：
```typescript
await pool.query(
  `INSERT INTO "Order" (..., "stripeSessionId", ...)
   VALUES (..., $4, ...)`,
  [..., invoice.id, ...]  // ✅ 存储的是 invoice ID (in_xxx)
)
```

### 2. 退款查找逻辑的问题

当前退款查找代码（第1296-1318行）：

```typescript
case 'charge.refunded': {
  const charge = event.data.object as any
  
  // ❌ 问题 1: 尝试用 payment_intent 查找
  let orderResult = await pool.query(
    'SELECT ... FROM "Order" WHERE "stripeSessionId" = $1',
    [charge.payment_intent]  // payment_intent 是 pi_xxx
  )
  
  // ✅ 问题 2: 如果没找到，尝试用 invoice 查找
  if (orderResult.rows.length === 0 && charge.invoice) {
    const invoiceId = typeof charge.invoice === 'string' 
      ? charge.invoice 
      : charge.invoice.id
    orderResult = await pool.query(
      'SELECT ... FROM "Order" WHERE "stripeSessionId" = $1',
      [invoiceId]  // invoice 是 in_xxx
    )
  }
  
  if (orderResult.rows.length === 0) {
    console.warn('No order found for charge refund:', charge.id)
    break
  }
}
```

### 3. 为什么找不到订单？

对于**首次订阅订单**（如本例）：

1. **订单存储的值**: `stripeSessionId = cs_xxx` (checkout session ID)
2. **第一次查找**: 使用 `payment_intent = pi_xxx` 查找
   - ❌ 失败：因为 `pi_xxx ≠ cs_xxx`
3. **第二次查找**: 使用 `charge.invoice` 查找
   - ❌ 可能失败：首次订阅的 charge 可能没有 `invoice` 字段，或者 invoice 不存在
   - 即使有 invoice，也可能不是创建订单时使用的那个 invoice

### 4. Stripe 对象关系链

```
首次订阅流程：
Checkout Session (cs_xxx) 
  └─> Payment Intent (pi_xxx)
       └─> Charge (ch_xxx)
            └─> Invoice (in_xxx) [可选，可能不存在]

续订/升级流程：
Invoice (in_xxx)
  └─> Payment Intent (pi_xxx)
       └─> Charge (ch_xxx)
```

## 解决方案

### 方案 1：通过 Payment Intent 查找 Checkout Session（推荐）

修改退款查找逻辑，通过 `payment_intent` 找到对应的 `checkout session`：

```typescript
case 'charge.refunded': {
  const charge = event.data.object as any
  
  let orderResult
  
  // 1. 首先尝试通过 invoice 查找（续订/升级订单）
  if (charge.invoice) {
    const invoiceId = typeof charge.invoice === 'string' 
      ? charge.invoice 
      : charge.invoice.id
    orderResult = await pool.query(
      'SELECT ... FROM "Order" WHERE "stripeSessionId" = $1',
      [invoiceId]
    )
  }
  
  // 2. 如果没找到，通过 payment_intent 查找 checkout session
  if ((!orderResult || orderResult.rows.length === 0) && charge.payment_intent) {
    try {
      // 通过 payment_intent 查找关联的 checkout session
      const sessions = await stripe.checkout.sessions.list({
        payment_intent: charge.payment_intent,
        limit: 1
      })
      
      if (sessions.data.length > 0) {
        orderResult = await pool.query(
          'SELECT ... FROM "Order" WHERE "stripeSessionId" = $1',
          [sessions.data[0].id]
        )
      }
    } catch (e) {
      console.warn('Failed to lookup order by payment_intent:', e)
    }
  }
  
  // 3. 如果还是没找到，记录详细日志
  if (!orderResult || orderResult.rows.length === 0) {
    console.warn('No order found for charge refund:', {
      chargeId: charge.id,
      paymentIntent: charge.payment_intent,
      invoice: charge.invoice,
      amount: charge.amount_refunded
    })
    break
  }
  
  // ... 后续处理逻辑
}
```

### 方案 2：在订单创建时额外存储 payment_intent

在创建订单时，除了存储 `stripeSessionId`，还存储 `payment_intent` ID：

**缺点**：
- 需要修改数据库 schema（添加新字段）
- 需要修改所有订单创建逻辑
- 增加存储成本

**优点**：
- 查找更直接
- 性能更好

### 方案 3：通过 Charge ID 反向查找

通过 charge ID 获取 payment_intent，再通过 payment_intent 查找 checkout session 或 invoice。

## 立即行动项

1. **执行 SQL 查询**：使用 `find_order_by_refund.sql` 中的查询找到对应的订单
2. **验证订单存在**：确认订单确实存在于数据库中
3. **修复 webhook 逻辑**：实施方案 1，通过 payment_intent 查找 checkout session
4. **增强日志**：在找不到订单时记录更多调试信息
5. **测试验证**：测试不同类型的订单退款场景

## 相关文件

- `removehandwriting-web/src/app/api/stripe/webhook/route.ts` (第1285-1318行)
- `find_order_by_refund.sql` (SQL 查询文件)
