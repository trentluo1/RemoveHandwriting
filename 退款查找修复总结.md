# Stripe Webhook 退款查找修复总结

## 修复日期
2026-01-08

## 问题描述
`charge.refunded` webhook 事件处理时，对于首次订阅订单和一次性支付订单无法找到对应的订单。

## 根本原因
- **续订/升级订单**：`stripeSessionId` 存储的是 `invoice.id` (in_xxx)，可以通过 `charge.invoice` 查找 ✅
- **首次订阅/一次性支付订单**：`stripeSessionId` 存储的是 `session.id` (cs_xxx)，但原代码没有通过 `payment_intent` 查找 `checkout session` 的逻辑 ❌

## 修复方案

### 修改文件
`removehandwriting-web/src/app/api/stripe/webhook/route.ts` (第1285-1373行)

### 修复内容

#### 1. 增强日志记录
- 在初始日志中添加 `hasInvoice` 和 `invoiceId` 字段
- 为每种查找方法添加详细的日志记录
- 在找不到订单时记录完整的调试信息

#### 2. 优化查找策略（按优先级）

**方法 1: 通过 invoice 查找**（续订/升级订单）
- 保持原有逻辑不变
- 确保续订和升级订单继续正常工作

**方法 2: 通过 payment_intent 查找 checkout session**（新增）
- 使用 `stripe.checkout.sessions.list({ payment_intent: ... })` 查找关联的 checkout session
- 然后用找到的 `session.id` 查找订单
- 解决首次订阅和一次性支付订单的查找问题

**方法 3: 直接通过 payment_intent 查找**（保留作为兼容）
- 保留原有逻辑作为降级方案
- 虽然不太可能匹配，但作为最后的尝试

### 代码变更

```typescript
// 修复前：只有两种查找方式
// 1. 通过 payment_intent 直接查找（失败）
// 2. 通过 invoice 查找（仅续订/升级订单成功）

// 修复后：三种查找方式
// 1. 通过 invoice 查找（续订/升级订单）✅
// 2. 通过 payment_intent 查找 checkout session（首次订阅/一次性支付）✅ 新增
// 3. 直接通过 payment_intent 查找（兼容）✅
```

## 修复效果

### 修复前
- ✅ 续订订单：能正常处理
- ✅ 升级订单：能正常处理
- ❌ 首次订阅订单：无法找到订单
- ❌ API/Credit/Custom 订单：无法找到订单

### 修复后
- ✅ 续订订单：能正常处理（保持不变）
- ✅ 升级订单：能正常处理（保持不变）
- ✅ 首次订阅订单：**现在能正常处理** 🎉
- ✅ API/Credit/Custom 订单：**现在能正常处理** 🎉

## 测试建议

1. **续订订单退款**：验证原有功能不受影响
2. **升级订单退款**：验证原有功能不受影响
3. **首次订阅订单退款**：验证新功能正常工作
4. **API/Credit/Custom 订单退款**：验证新功能正常工作

## 相关文件

- `removehandwriting-web/src/app/api/stripe/webhook/route.ts` - 修复的代码文件
- `退款订单查找错误分析.md` - 问题分析文档
- `退款查找逻辑分析.md` - 逻辑分析文档
- `find_order_by_refund.sql` - SQL 查询文件

## 注意事项

1. 修复后的代码会调用 Stripe API 的 `checkout.sessions.list` 方法，确保有足够的 API 配额
2. 如果 Stripe API 调用失败，会记录警告日志但不会中断处理流程
3. 所有查找方法都有错误处理，确保单个方法失败不会影响其他方法
