# 修复失败的退款订单 - 完整指南

## 订单信息

- **Checkout Session ID**: `cs_live_a1iTZaKXJMyTQkIuw5LVhKeLYNgjunOWrIeK8aHuFUhhdY1ASduv6yNtxf`
- **Payment Intent**: `pi_3SLdWIHEruuV5CvF1vs1b8xU`
- **Charge ID**: `ch_3SLdWIHEruuV5CvF1Ekj8bPC`
- **退款金额**: 2780 分 ($27.80)
- **Customer ID**: `cus_TIDyHolVhQ1501`
- **Customer Email**: `kontakt@schauertegmbh.de`
- **订单类型**: 首次订阅 (subscription)

## 修复步骤

### 步骤 1: 检查订单当前状态

执行以下 SQL 查看订单状态：

```sql
SELECT 
  id,
  "userId",
  amount,
  "refundStatus",
  "refundType",
  "refundedAmount",
  "orderType",
  "billingEmail"
FROM "Order"
WHERE "stripeSessionId" = 'cs_live_a1iTZaKXJMyTQkIuw5LVhKeLYNgjunOWrIeK8aHuFUhhdY1ASduv6yNtxf';
```

**预期结果**：
- `refundStatus` 应该是 `NULL` 或不是 `'refunded'`
- 如果已经是 `'refunded'`，说明可能已经通过其他方式处理过了

### 步骤 2: 更新订单退款状态

```sql
UPDATE "Order" 
SET 
  "refundStatus" = 'refunded',
  "refundType" = 'real',
  "refundedAmount" = 2780,
  "refundedAt" = NOW(),
  "refundReason" = 'Customer requested refund (manually fixed after webhook fix)'
WHERE "stripeSessionId" = 'cs_live_a1iTZaKXJMyTQkIuw5LVhKeLYNgjunOWrIeK8aHuFUhhdY1ASduv6yNtxf'
  AND ("refundStatus" IS NULL OR "refundStatus" != 'refunded');
```

### 步骤 3: 更新订阅状态

由于这是首次订阅订单，退款后应该取消订阅：

```sql
UPDATE "Subscription"
SET 
  status = 'canceled',
  "currentPeriodEnd" = NOW(),
  "updatedAt" = NOW()
WHERE "userId" = (
  SELECT "userId" 
  FROM "Order" 
  WHERE "stripeSessionId" = 'cs_live_a1iTZaKXJMyTQkIuw5LVhKeLYNgjunOWrIeK8aHuFUhhdY1ASduv6yNtxf'
)
  AND status != 'canceled';
```

### 步骤 4: 取消 Stripe 订阅（可选但推荐）

如果 Stripe 中的订阅仍然活跃，需要手动取消：

**方法 A: 使用 Stripe Dashboard**
1. 登录 Stripe Dashboard
2. 搜索 Customer ID: `cus_TIDyHolVhQ1501`
3. 找到活跃的订阅并取消

**方法 B: 使用 Stripe CLI**
```bash
stripe subscriptions list --customer cus_TIDyHolVhQ1501
# 找到订阅 ID 后
stripe subscriptions cancel sub_1SLdWKHEruuV5CvF70FmZNrc
```

**方法 C: 使用代码（如果需要自动化）**
可以创建一个临时脚本或使用现有的管理接口。

### 步骤 5: 发送退款邮件（可选）

Webhook 代码会自动发送退款邮件，但由于这是手动修复，可能需要手动发送。

**选项 A: 等待下次相关事件触发**（如果系统会自动发送）

**选项 B: 手动发送邮件**
- 用户邮件：发送到 `kontakt@schauertegmbh.de`
- 管理员邮件：发送到 `service@removehandwriting.com`

### 步骤 6: 验证修复结果

```sql
SELECT 
  o.id as order_id,
  o."refundStatus",
  o."refundType",
  o."refundedAmount",
  o."refundedAt",
  o."refundReason",
  s.status as subscription_status,
  s."currentPeriodEnd",
  s."stripeCustomerId"
FROM "Order" o
LEFT JOIN "Subscription" s ON o."userId" = s."userId"
WHERE o."stripeSessionId" = 'cs_live_a1iTZaKXJMyTQkIuw5LVhKeLYNgjunOWrIeK8aHuFUhhdY1ASduv6yNtxf';
```

**预期结果**：
- `refundStatus` = `'refunded'`
- `refundType` = `'real'`
- `refundedAmount` = `2780`
- `refundedAt` = 当前时间
- `subscription_status` = `'canceled'`

## 完整 SQL 脚本（一次性执行）

如果需要一次性执行所有步骤，可以使用以下脚本（在事务中执行）：

```sql
BEGIN;

-- 更新订单退款状态
UPDATE "Order" 
SET 
  "refundStatus" = 'refunded',
  "refundType" = 'real',
  "refundedAmount" = 2780,
  "refundedAt" = NOW(),
  "refundReason" = 'Customer requested refund (manually fixed after webhook fix)'
WHERE "stripeSessionId" = 'cs_live_a1iTZaKXJMyTQkIuw5LVhKeLYNgjunOWrIeK8aHuFUhhdY1ASduv6yNtxf'
  AND ("refundStatus" IS NULL OR "refundStatus" != 'refunded');

-- 更新订阅状态
UPDATE "Subscription"
SET 
  status = 'canceled',
  "currentPeriodEnd" = NOW(),
  "updatedAt" = NOW()
WHERE "userId" = (
  SELECT "userId" 
  FROM "Order" 
  WHERE "stripeSessionId" = 'cs_live_a1iTZaKXJMyTQkIuw5LVhKeLYNgjunOWrIeK8aHuFUhhdY1ASduv6yNtxf'
)
  AND status != 'canceled';

-- 如果确认无误，提交事务
COMMIT;

-- 如果发现问题，可以回滚
-- ROLLBACK;
```

## 注意事项

1. **幂等性**：SQL 语句已经包含了幂等性检查，重复执行不会重复更新
2. **Stripe 订阅**：数据库更新后，建议也取消 Stripe 中的订阅，保持数据一致性
3. **邮件通知**：如果需要发送邮件，可以手动触发或等待系统自动发送
4. **测试环境**：建议先在测试环境验证 SQL 语句

## 相关文件

- `修复失败订单-快速执行.sql` - 快速执行版本
- `修复失败的退款订单.sql` - 详细版本（包含更多验证查询）
