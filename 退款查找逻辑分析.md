# 退款查找逻辑分析 - 为什么大部分订单能正常处理？

## 当前查找逻辑

```typescript
// 第一次查找：用 payment_intent
let orderResult = await pool.query(
  'SELECT ... FROM "Order" WHERE "stripeSessionId" = $1',
  [charge.payment_intent]  // pi_xxx
)

// 第二次查找：用 invoice（如果存在）
if (orderResult.rows.length === 0 && charge.invoice) {
  orderResult = await pool.query(
    'SELECT ... FROM "Order" WHERE "stripeSessionId" = $1',
    [invoiceId]  // in_xxx
  )
}
```

## 订单类型和存储值

### 1. 首次订阅订单（subscription）
- **存储值**: `stripeSessionId = session.id` (cs_xxx)
- **创建位置**: `checkout.session.completed` 事件（第552行）
- **退款查找**:
  - 第一次：用 `payment_intent` (pi_xxx) → ❌ 失败（pi_xxx ≠ cs_xxx）
  - 第二次：用 `invoice` (in_xxx) → ❌ 失败（in_xxx ≠ cs_xxx，且首次订阅的 charge 可能没有 invoice）
- **结果**: ❌ **找不到订单**

### 2. 续订订单（renewal）
- **存储值**: `stripeSessionId = invoice.id` (in_xxx)
- **创建位置**: `invoice.payment_succeeded` 事件，billing_reason='subscription_cycle'（第944行）
- **退款查找**:
  - 第一次：用 `payment_intent` (pi_xxx) → ❌ 失败（pi_xxx ≠ in_xxx）
  - 第二次：用 `invoice` (in_xxx) → ✅ **成功**（in_xxx = in_xxx）
- **结果**: ✅ **能找到订单**

### 3. 升级订单（upgrade）
- **存储值**: `stripeSessionId = invoice.id` (in_xxx)
- **创建位置**: `invoice.payment_succeeded` 事件，billing_reason='subscription_update'（第1031行）
- **退款查找**:
  - 第一次：用 `payment_intent` (pi_xxx) → ❌ 失败（pi_xxx ≠ in_xxx）
  - 第二次：用 `invoice` (in_xxx) → ✅ **成功**（in_xxx = in_xxx）
- **结果**: ✅ **能找到订单**

### 4. API/Credit/Custom 订单
- **存储值**: `stripeSessionId = session.id` (cs_xxx)
- **创建位置**: `checkout.session.completed` 事件（第159、250、358行）
- **退款查找**:
  - 第一次：用 `payment_intent` (pi_xxx) → ❌ 失败（pi_xxx ≠ cs_xxx）
  - 第二次：用 `invoice` (in_xxx) → ❌ 失败（这些是一次性支付，没有 invoice）
- **结果**: ❌ **找不到订单**

## 总结

| 订单类型 | stripeSessionId 存储值 | 第一次查找 | 第二次查找 | 结果 |
|---------|----------------------|----------|----------|------|
| 首次订阅 | cs_xxx (session.id) | ❌ pi_xxx ≠ cs_xxx | ❌ in_xxx ≠ cs_xxx | ❌ 失败 |
| 续订 | in_xxx (invoice.id) | ❌ pi_xxx ≠ in_xxx | ✅ in_xxx = in_xxx | ✅ 成功 |
| 升级 | in_xxx (invoice.id) | ❌ pi_xxx ≠ in_xxx | ✅ in_xxx = in_xxx | ✅ 成功 |
| API/Credit/Custom | cs_xxx (session.id) | ❌ pi_xxx ≠ cs_xxx | ❌ 无 invoice | ❌ 失败 |

## 为什么大部分订单能正常处理？

因为：
- **续订订单**和**升级订单**占大部分（订阅的持续付费）
- 这两种订单的 `stripeSessionId` 存储的是 `invoice.id`
- `charge.refunded` 事件中的 `charge.invoice` 正好匹配
- 所以第二次查找能成功 ✅

## 哪些订单会失败？

- **首次订阅订单**：存储的是 `session.id`，但查找时没有 session ID
- **API/Credit/Custom 一次性支付订单**：存储的是 `session.id`，且没有 invoice

## 解决方案

需要添加第三种查找方式：**通过 payment_intent 查找 checkout session**

```typescript
// 1. 先尝试 invoice（续订/升级订单）
if (charge.invoice) { ... }

// 2. 如果没找到，通过 payment_intent 查找 checkout session（首次订阅/一次性支付）
if (!orderResult && charge.payment_intent) {
  const sessions = await stripe.checkout.sessions.list({
    payment_intent: charge.payment_intent,
    limit: 1
  })
  if (sessions.data.length > 0) {
    orderResult = await pool.query(
      'SELECT ... FROM "Order" WHERE "stripeSessionId" = $1',
      [sessions.data[0].id]  // cs_xxx
    )
  }
}
```

这样就能覆盖所有类型的订单了！
