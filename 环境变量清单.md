# 项目环境变量清单

本文档列出了 RemoveHandwriting 项目中使用的所有环境变量。

## 📋 目录

- [Web 应用环境变量](#web-应用环境变量)
- [移动应用环境变量](#移动应用环境变量)
- [环境变量分类说明](#环境变量分类说明)

---

## Web 应用环境变量

### 🔐 Supabase 配置

| 变量名 | 类型 | 必需 | 说明 |
|--------|------|------|------|
| `NEXT_PUBLIC_SUPABASE_URL` | 公开 | ✅ | Supabase 项目 URL |
| `NEXT_PUBLIC_SUPABASE_ANON_KEY` | 公开 | ✅ | Supabase 匿名密钥（前端使用） |
| `SUPABASE_SERVICE_ROLE_KEY` | 私有 | ✅ | Supabase 服务角色密钥（后端使用） |

### 🗄️ 数据库配置

| 变量名 | 类型 | 必需 | 说明 |
|--------|------|------|------|
| `DATABASE_URL` | 私有 | ✅ | PostgreSQL 数据库连接字符串 |
| `DIRECT_URL` | 私有 | ✅ | 直接数据库连接字符串（用于 Prisma） |

### 💳 Stripe 支付配置

| 变量名 | 类型 | 必需 | 说明 |
|--------|------|------|------|
| `STRIPE_SECRET_KEY` | 私有 | ✅ | Stripe 密钥（sk_test_ 或 sk_live_） |
| `NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY` | 公开 | ✅ | Stripe 公开密钥（前端使用） |
| `STRIPE_WEBHOOK_SECRET` | 私有 | ✅ | Stripe Webhook 签名密钥 |

### 💰 PayPal 支付配置

| 变量名 | 类型 | 必需 | 说明 |
|--------|------|------|------|
| `PAYPAL_CLIENT_ID` | 私有 | ✅ | PayPal 客户端 ID |
| `PAYPAL_CLIENT_SECRET` | 私有 | ✅ | PayPal 客户端密钥 |
| `PAYPAL_MODE` | 私有 | ⚠️ | PayPal 模式：`sandbox` 或 `live`（默认：`sandbox`） |
| `PAYPAL_PLAN_MONTHLY` | 私有 | ⚠️ | PayPal 月度订阅计划 ID（可选） |
| `PAYPAL_PLAN_QUARTERLY` | 私有 | ⚠️ | PayPal 季度订阅计划 ID（可选） |
| `PAYPAL_PLAN_YEARLY` | 私有 | ⚠️ | PayPal 年度订阅计划 ID（可选） |
| `PAYPAL_WEBHOOK_ID` | 私有 | ⚠️ | PayPal Webhook ID（用于签名验证） |
| `PAYPAL_SKIP_WEBHOOK_VERIFICATION` | 私有 | ⚠️ | 跳过 Webhook 验证（测试用，设为 `true`） |

### 🖼️ 图像处理 API 配置

#### 阿里云 API（直接调用模式）

| 变量名 | 类型 | 必需 | 说明 |
|--------|------|------|------|
| `ALIYUN_API_HOST` | 私有 | ⚠️ | 阿里云 API 主机地址（如：`https://sjccup.market.alicloudapi.com`） |
| `ALIYUN_API_PATH` | 私有 | ⚠️ | 阿里云 API 路径（如：`/sjccup`） |
| `ALIYUN_APPCODE` | 私有 | ⚠️ | 阿里云 AppCode |
| `USE_ALIYUN_PROXY` | 私有 | ⚠️ | 是否使用代理模式（`false` 为直接调用，`true` 为代理） |

#### 阿里云 API（代理模式）

| 变量名 | 类型 | 必需 | 说明 |
|--------|------|------|------|
| `ALIYUN_PROXY_HOST` | 私有 | ⚠️ | 阿里云代理服务器地址 |
| `ALIYUN_PROXY_TOKEN` | 私有 | ⚠️ | 代理服务器认证令牌 |
| `USE_ALIYUN_PROXY` | 私有 | ⚠️ | 必须设为 `true` |

#### 百度智能云 API

| 变量名 | 类型 | 必需 | 说明 |
|--------|------|------|------|
| `BAIDU_API_KEY` | 私有 | ⚠️ | 百度 API Key |
| `BAIDU_SECRET_KEY` | 私有 | ⚠️ | 百度 Secret Key |

#### 通用配置

| 变量名 | 类型 | 必需 | 说明 |
|--------|------|------|------|
| `IMAGE_PROCESSING_PROVIDER` | 私有 | ⚠️ | 图像处理提供商：`aliyun` 或 `baidu`（默认：`aliyun`） |

**注意**：阿里云和百度 API 配置二选一即可。

### 📧 SMTP 邮件配置

| 变量名 | 类型 | 必需 | 说明 |
|--------|------|------|------|
| `SMTP_HOST` | 私有 | ✅ | SMTP 服务器地址（如：`smtp.qiye.aliyun.com`） |
| `SMTP_PORT` | 私有 | ⚠️ | SMTP 端口（默认：`465`） |
| `SMTP_SECURE` | 私有 | ⚠️ | 是否使用 SSL/TLS（`true` 或 `false`，默认：`true`） |
| `SMTP_USER` | 私有 | ✅ | SMTP 用户名（邮箱地址） |
| `SMTP_PASS` | 私有 | ✅ | SMTP 密码或授权码 |
| `EMAIL_FROM` | 私有 | ⚠️ | 发件人地址和名称（如：`RemoveHandwriting <service@removehandwriting.com>`） |

### 🛡️ Cloudflare Turnstile 配置

| 变量名 | 类型 | 必需 | 说明 |
|--------|------|------|------|
| `NEXT_PUBLIC_CLOUDFLARE_TURNSTILE_SITE_KEY` | 公开 | ⚠️ | Cloudflare Turnstile Site Key（前端使用） |
| `CLOUDFLARE_TURNSTILE_SECRET_KEY` | 私有 | ⚠️ | Cloudflare Turnstile Secret Key（后端验证） |

### 🔴 Redis 配置

| 变量名 | 类型 | 必需 | 说明 |
|--------|------|------|------|
| `RemoveHandwriting_REDIS_URL` | 私有 | ⚠️ | Redis 连接字符串（格式：`redis://default:password@hostname:6379`） |

### 🌐 应用 URL 配置

| 变量名 | 类型 | 必需 | 说明 |
|--------|------|------|------|
| `NEXT_PUBLIC_APP_URL` | 公开 | ⚠️ | 应用基础 URL（如：`https://removehandwriting.com`，默认：`https://removehandwriting.com`） |
| `NEXT_PUBLIC_SITE_URL` | 公开 | ⚠️ | 网站 URL（用于 SEO，默认：`https://removehandwriting.com`） |
| `NEXT_PUBLIC_API_DOMAIN` | 公开 | ⚠️ | API 域名（用于 API Key 管理） |

### 👨‍💼 管理员配置

| 变量名 | 类型 | 必需 | 说明 |
|--------|------|------|------|
| `NEXT_PUBLIC_ADMIN_EMAILS` | 公开 | ⚠️ | 管理员邮箱列表（逗号分隔，如：`admin1@example.com,admin2@example.com`） |

### ⚡ 限流配置

| 变量名 | 类型 | 必需 | 说明 |
|--------|------|------|------|
| `RATE_LIMIT_PER_MINUTE` | 私有 | ⚠️ | 每分钟请求限制（默认：`5`） |
| `RATE_LIMIT_PER_HOUR` | 私有 | ⚠️ | 每小时请求限制（默认：`30`） |

### 🐛 调试配置

| 变量名 | 类型 | 必需 | 说明 |
|--------|------|------|------|
| `NEXT_PUBLIC_LOG_LEVEL` | 公开 | ⚠️ | 日志级别（用于前端调试） |
| `NODE_ENV` | 系统 | ⚠️ | Node.js 环境（`development`、`production` 或 `test`） |

---

## 移动应用环境变量

移动应用（Flutter）使用编译时环境变量，通过 `--dart-define` 参数传递。

### 🔐 Supabase 配置

| 变量名 | 类型 | 必需 | 说明 |
|--------|------|------|------|
| `SUPABASE_URL` | 编译时 | ⚠️ | Supabase 项目 URL（有默认值） |
| `SUPABASE_ANON_KEY` | 编译时 | ⚠️ | Supabase 匿名密钥（有默认值） |

### 🌐 API 配置

| 变量名 | 类型 | 必需 | 说明 |
|--------|------|------|------|
| `API_BASE_URL` | 编译时 | ⚠️ | API 基础 URL（默认：`https://removehandwriting.com`） |

### 📱 应用内购买产品 ID

#### iOS 产品 ID

| 变量名 | 类型 | 必需 | 说明 |
|--------|------|------|------|
| `IOS_PRODUCT_ID_MONTHLY` | 编译时 | ⚠️ | iOS 月度订阅产品 ID（默认：`com.removehandwriting.monthly`） |
| `IOS_PRODUCT_ID_QUARTERLY` | 编译时 | ⚠️ | iOS 季度订阅产品 ID（默认：`com.removehandwriting.quarterly`） |
| `IOS_PRODUCT_ID_YEARLY` | 编译时 | ⚠️ | iOS 年度订阅产品 ID（默认：`com.removehandwriting.yearly`） |

#### Android 产品 ID

| 变量名 | 类型 | 必需 | 说明 |
|--------|------|------|------|
| `ANDROID_PRODUCT_ID_MONTHLY` | 编译时 | ⚠️ | Android 月度订阅产品 ID（默认：`monthly_subscription`） |
| `ANDROID_PRODUCT_ID_QUARTERLY` | 编译时 | ⚠️ | Android 季度订阅产品 ID（默认：`quarterly_subscription`） |
| `ANDROID_PRODUCT_ID_YEARLY` | 编译时 | ⚠️ | Android 年度订阅产品 ID（默认：`yearly_subscription`） |

### 🔑 管理员密钥

| 变量名 | 类型 | 必需 | 说明 |
|--------|------|------|------|
| `ADMIN_SECRET` | 编译时 | ⚠️ | 管理员密钥（用于匿名用户接口认证，有默认值） |

---

## 环境变量分类说明

### 变量类型说明

- **公开变量**（`NEXT_PUBLIC_*`）：这些变量会被打包到前端代码中，可以在浏览器中访问。**不要在其中存储敏感信息**。
- **私有变量**：这些变量只在服务器端使用，不会暴露给客户端。
- **编译时变量**（移动应用）：在编译 Flutter 应用时通过 `--dart-define` 传递。

### 必需性说明

- ✅ **必需**：缺少这些变量会导致应用无法正常运行
- ⚠️ **可选**：有默认值或仅在特定功能中使用

### 使用示例

#### Web 应用（`.env.local`）

```bash
# Supabase
NEXT_PUBLIC_SUPABASE_URL=https://your-project.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=your_anon_key
SUPABASE_SERVICE_ROLE_KEY=your_service_role_key

# 数据库
DATABASE_URL=postgresql://postgres:password@host:5432/postgres
DIRECT_URL=postgresql://postgres:password@host:5432/postgres

# Stripe
STRIPE_SECRET_KEY=sk_test_xxx
NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY=pk_test_xxx
STRIPE_WEBHOOK_SECRET=whsec_xxx

# PayPal
PAYPAL_CLIENT_ID=your_client_id
PAYPAL_CLIENT_SECRET=your_client_secret
PAYPAL_MODE=sandbox

# 阿里云 API（直接调用）
ALIYUN_API_HOST=https://sjccup.market.alicloudapi.com
ALIYUN_API_PATH=/sjccup
ALIYUN_APPCODE=your_appcode
IMAGE_PROCESSING_PROVIDER=aliyun
USE_ALIYUN_PROXY=false

# SMTP
SMTP_HOST=smtp.qiye.aliyun.com
SMTP_PORT=465
SMTP_SECURE=true
SMTP_USER=service@removehandwriting.com
SMTP_PASS=your_password
EMAIL_FROM=RemoveHandwriting <service@removehandwriting.com>

# 应用 URL
NEXT_PUBLIC_APP_URL=https://removehandwriting.com
NEXT_PUBLIC_SITE_URL=https://removehandwriting.com

# 管理员
NEXT_PUBLIC_ADMIN_EMAILS=admin@example.com

# Redis（可选）
RemoveHandwriting_REDIS_URL=redis://default:password@host:6379
```

#### 移动应用（编译时）

```bash
# iOS
flutter build ios --dart-define=SUPABASE_URL=https://your-project.supabase.co \
  --dart-define=SUPABASE_ANON_KEY=your_anon_key

# Android
flutter build apk --dart-define=SUPABASE_URL=https://your-project.supabase.co \
  --dart-define=SUPABASE_ANON_KEY=your_anon_key
```

---

## 📝 注意事项

1. **安全性**：
   - 永远不要将包含敏感信息的 `.env.local` 文件提交到版本控制系统
   - 使用 `NEXT_PUBLIC_*` 前缀的变量会被暴露给客户端，不要存储密钥
   - 定期轮换密钥和密码

2. **部署**：
   - 在 Vercel 或其他部署平台中，需要在项目设置中配置所有必需的环境变量
   - 确保生产环境和开发环境使用不同的密钥

3. **移动应用**：
   - 移动应用的环境变量在编译时注入，需要为每个构建配置不同的值
   - 考虑使用 CI/CD 管道自动注入环境变量

---

**最后更新**：2025-01-XX
**维护者**：RemoveHandwriting 开发团队

